# verl nightly Ascend CI watch — 2026-08-19

Window: nightly_ascend.yml runs of 2026-08-18 (17:28 / 18:27 / 19:30 UTC). Two
runs failed (32165790481, 32176994075), one cancelled (32171124648). All six
failed jobs analyzed; **no VERL_BUG found — no draft PRs opened today.** Three
jobs are `check_npu.py` baseline regressions (action: regenerate baselines on
the CI runners — they are runner-side artifacts, not in the repo), two died on
GitHub-side 429 rate limiting before any step ran, and one hit a missing
pre-provisioned model on its runner host.

## Summary

| job | run | verdict | action |
|---|---|---|---|
| nightlyCI_grpo-qwen3-30b-veomni-fsdp_ascend | 32176994075 (job 95841701621) | INFRA | none — GitHub 429 on action download, died pre-steps |
| nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend | 32176994075 (job 95841701810) | INFRA | provision `Qwen/Qwen3.5-2B` on the a2b3-8 host (CI admins), or move job to a3-16 (maintainer call) |
| nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend | 32176994075 (job 95841701885) | INFRA | none — same 429; known OOM signature did NOT recur (job died earlier) |
| nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend | 32165790481 (job 95804945499) | BASELINE_REGRESSION | regenerate `baseline_qwen3_vl_8b_Instruct_fsdp2_npu.txt` on the runner |
| nightlyCI_dapo-moonlight-16b-megatron-vllm_ascend | 32165790481 (job 95804945606) | BASELINE_REGRESSION | regenerate `baseline_dapo_moonlight-16b_megatron_npu.txt` on the runner |
| nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend | 32165790481 (job 95804945667) | BASELINE_REGRESSION | regenerate `baseline_ppo_qwen3-8b_fsdp_npu.txt` on the runner |

## New failures

### nightlyCI_grpo-qwen3-30b-veomni-fsdp_ascend — run 32176994075, job 95841701621

Died 2026-08-18T20:16–20:17Z before any workflow step executed: the runner could
not download `actions/checkout@v4` from codeload.github.com — HTTP 429 three
times, then gave up.

`r32176994075_nightlyCI_grpo-qwen3-30b-veomni-fsdp_ascend.log:18-19`
```
##[error]Response status code does not indicate success: 429 (Too Many Requests).
##[error]Failed to download archive 'https://codeload.github.com/actions/checkout/tar.gz/11d5960...' after 3 attempts.
```

**Verdict INFRA:** GitHub-side rate limiting / network egress from the self-hosted
runner fleet; nothing in verl is reachable or broken. Re-run should clear it.

### nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend — run 32176994075, job 95841701810

New job (added Aug 17 in d4701e4e / #7362 "[ci, trainer] feat: add fsdpturbo
backend engine support"); **this was its first-ever execution** (the Aug 17 19:31
run started it but was cancelled at 19:36). It ran on the
`linux-aarch64-a2b3-8` pool / `latest-vllm-910b-ubuntu` image, host
`linux-aarch64-a2b3-8-jn9vs`. Training never started: tokenizer load failed
because the local model path does not exist on that host.

`r32176994075_nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend.log:1999`
```
huggingface_hub.errors.HFValidationError: Repo id must be in the form 'repo_name' or 'namespace/repo_name': '/root/.cache/models/Qwen/Qwen3.5-2B'. Use `repo_type` argument if needed.
```
`...log:2091-2093`
```
hydra.errors.InstantiationException: Error in call to target 'verl.workers.config.model.HFModelConfig':
OSError("Repo id must be in the form ...: '/root/.cache/models/Qwen/Qwen3.5-2B'. ...")
full_key: actor_rollout_ref.model
```

Chain: `trainer_base._init_tokenizer` → `HFModelConfig.__post_init__`
(`verl/workers/config/model.py:161`) → `hf_tokenizer` → transformers
`AutoTokenizer.from_pretrained`, which — finding no local directory at
`/root/.cache/models/Qwen/Qwen3.5-2B` — treats the path as a hub repo id and
fails validation.

**Verdict INFRA, not VERL_BUG:**
- The script (`tests/special_npu/nightly_ci_ascend/run_grpo_qwen3_5_2b_fsdp_turbo_npu.sh:9`)
  uses the same `MODEL_PATH=${HOME}/.cache/models/${MODEL_ID}` convention as its
  siblings (`run_grpo_qwen3_5_2b_fsdp2_npu.sh:10`,
  `run_gspo_qwen3_8b_fsdp2_npu.sh:15`), and no nightly job downloads weights —
  every "Prepare weights" step is only `ln -s /root/.cache/models ~/models`.
- The CI guide lists `Qwen3.5-2B` at exactly that path among the pre-provisioned
  models (`docs/ascend_tutorial/contribution_guide/ascend_ci_guide_zh.rst:54`),
  i.e. verl's config conforms to its documented environment contract.
- The model demonstrably exists in CI-land: `nightlyCI_grpo_qwen3_5_2b_fsdp2_vllm_ascend`
  (same model, same path convention, a3-16 pool) **succeeded on Aug 13**
  (run 31730028598). The failure is specific to the a2b3-8-jn9vs host's cache:
  the gspo-qwen3-8b job on the *same host* the same evening loaded Qwen3-8B fine
  (it died later for unrelated 429 reasons — see below), so the host has models,
  just not this one.

Action for CI admins: provision `Qwen/Qwen3.5-2B` on the a2b3-8 pool (or, if
FSDP-Turbo is meant for A3, maintainers move the job back to `linux-aarch64-a3-16`
— that changes hardware coverage, so deliberately not auto-drafted).
Upstream dedup: `gh search` for "Qwen3.5-2B" / "fsdp_turbo" found no matching
open issue/PR.

### nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend — run 32176994075, job 95841701885

Died 2026-08-18T20:12–20:13Z, same pre-step 429 failure as the veomni job
(runner `linux-aarch64-a2b3-8-jn9vs-runner-6rpvj`):

`r32176994075_nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend.log:18-19`
```
##[error]Response status code does not indicate success: 429 (Too Many Requests).
##[error]Failed to download archive '.../actions/checkout/tar.gz/11d5960...' after 3 attempts.
```

**Verdict INFRA.** Note for continuity: the known signature
`ray-oom-workers-killed-low-node-memory@actor_rollout_init_model` (seen Aug 16/17,
see `reports/backfill-2026-08-17.md`) **did not recur tonight** — the job never
got past action download. The backfill watch item (NGPUS_PER_NODE=16 default vs
the 8-NPU a2b3-8 host after #7444) therefore remains untested; no new evidence,
so still no auto-draft.

### nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend — run 32165790481, job 95804945499

Training **completed** (15/15 steps, 23:46; final val acc@1 0.499). The failing
step is the checking script
(`check_npu.py --log run_qwen3_vl_8b_Instruct_fsdp2_npu.log --base .../baseline_qwen3_vl_8b_Instruct_fsdp2_npu.txt`):

`r32165790481_nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend.log:2557-2561`
```
【内存】actor/perf/max_memory_allocated_gb  | A=44.537365 B=39.297200 | 差值=5.240165
【性能】perf/throughput                      | A=164.241863 B=87.646000 | 变化=87.39%
【性能】timing_s/step                        | A=87.837079 B=410.057200 | 变化=-78.58%
【精度】critic/rewards/mean                  | A=0.384750 B=-0.855600 | 差值=1.240350
【精度】training/rollout_probs_diff_mean     | A=0.005527 B=0.023700 | 变化=-76.68%
```
(A = tonight's run, B = on-runner baseline; A confirmed against the run's own
`step:15` metrics line, `...log:2536`.)

**Verdict BASELINE_REGRESSION.** The run is ~2× *faster* than the baseline with
lower rollout/training prob divergence and a very different rewards mean — i.e.
the baseline reflects a substantially older configuration of this job (the
`latest-vllm-910b-ubuntu` image tag is moving), not a degradation introduced by
verl code. No verl commit touches this path in a way that would both double
throughput and change reward scale; the fix is regenerating the baseline file on
the runner (baselines and `check_npu.py` live only under
`/root/.cache/nightly_log/`, not in the repo — nothing to PR).
Secondary observation: `actor/perf/cpu_memory_used_gb:504.5` and a
`RuntimeError: DataLoader worker (pid 47337) is killed by signal: Killed`
(`...log:2535`) fired at shutdown after the last step — host-RAM pressure on the
a2b3-8 hosts (same theme as the backfill's gspo OOM), but it did not fail any
step; logged as a watch item, not a verdict driver.

### nightlyCI_dapo-moonlight-16b-megatron-vllm_ascend — run 32165790481, job 95804945606

Training completed (15/15, 1:51:35). Failing step is again `check_npu.py`
(`--base .../baseline_dapo_moonlight-16b_megatron_npu.txt`):

`r32165790481_nightlyCI_dapo-moonlight-16b-megatron-vllm_ascend.log:4527-4536`
```
【内存】actor/perf/max_memory_allocated_gb  | A=39.017913 B=38.880700 | 差值=0.137213   (OK)
【性能】perf/throughput                      | A=97.454485 B=86.976111 | 变化=12.05%
【性能】timing_s/step                        | A=443.701911 B=413.003610 | 变化=7.43%
【精度】critic/rewards/mean                  | A=-0.861185 B=-0.857914 | 差值=-0.003271 (OK)
【精度】training/rollout_probs_diff_mean     | A=0.020218 B=0.025167 | 变化=-19.66%
```

**Verdict BASELINE_REGRESSION** — memory and reward within thresholds; pure
perf-metric drift (throughput +12%, step +7.4%, probs_diff −19.7%) beyond the ±5%
band. The `DataLoader worker ... killed by signal: Killed` here fired inside an
`atexit` callback (`_npu_shutdown`, `...log:4502-4508`), i.e. purely at teardown,
with `cpu_memory_used_gb` ≈ 543 — same host-RAM-pressure watch item, not the
failing step. Action: regenerate the baseline on the runner.

### nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend — run 32165790481, job 95804945667

Training completed (15/15, 19:14). Failing step is `check_npu.py`
(`--base .../baseline_ppo_qwen3-8b_fsdp_npu.txt`):

`r32165790481_nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend.log:2432-2436`
```
【内存】actor/perf/max_memory_allocated_gb  | A=22.215596 B=22.231838 | 差值=-0.016242 (OK)
【性能】perf/throughput                      | A=68.556178 B=47.638867 | 变化=43.91%
【性能】timing_s/step                        | A=73.782400 B=107.026360 | 变化=-31.06%
【精度】critic/rewards/mean                  | A=0.381250 B=0.332102 | 差值=0.049148   (OK, threshold 0.05)
【精度】training/rollout_probs_diff_mean     | A=0.004511 B=0.004396 | 变化=2.61%      (OK)
```

**Verdict BASELINE_REGRESSION.** Only the two perf metrics are out, both in the
"faster" direction (+44% throughput, −31% step time); memory is byte-for-byte
identical and both accuracy metrics are inside thresholds. The current run is
strictly better than a stale baseline — refresh the baseline file on the runner.
Same shutdown-time DataLoader SIGKILL (CPU RSS ≈ 587 GB, `...log:2409,2410`) —
watch item.

**Cross-job context for the trio (vl/dapo/ppo):** these three share the '0 17'
schedule slot and have failed identically in every *completed* run since at
least **Aug 13** (31725296734, 31823495249) — the intervening Aug 15–17 slots
were cancelled, so last night is the first night this watch saw their logs.
All three ran on host `linux-aarch64-a2b3-8-jn9vs` (node159x86).

## Still failing

- `gspo-qwen3-8b-fsdp2-vllm_ascend::ray-oom-workers-killed-low-node-memory` —
  **not observed tonight** (job died at action download, before training).
  firstSeen 2026-08-17, lastSeen 2026-08-18, count 2. Kept in state; prune after
  14 days without recurrence.

## Draft PRs opened

None. No job came back VERL_BUG:
- The two 429 failures are GitHub-side; nothing to fix in verl.
- The fsdp_turbo job's missing weights is a runner-provisioning gap; verl's
  workflow/scripts conform to the documented provisioning contract, and moving
  the job between hardware pools is a maintainer decision, not an auto-draft.
- The three baseline regressions need runner-side baseline files regenerated;
  `check_npu.py` and `baseline_*.txt` are not in the verl repo
  (`rg baseline tests/special_npu .github/workflows/nightly_ascend.yml` → only
  a comment), so there is nothing to PR.

## Infra noise

- 32171124648 — cancelled 2026-08-18 18:27Z ('0 18' slot; all six of its jobs
  cancelled mid-run, incl. the qwen3_5_2b fsdp2 job — no completed runs to judge).
- 429 rate limiting on codeload.github.com hit the 19:30 slot twice (jobs 1 and
  3 above) — worth watching; if it recurs nightly, runner admins may want an
  action-cache mirror or a retry wrapper.
- Shutdown-time `DataLoader worker killed by signal: Killed` on all three '0 17'
  jobs with `actor|critic/perf/cpu_memory_used_gb` ≈ 505–588 GB — host RAM
  running near capacity on `linux-aarch64-a2b3-8-jn9vs` (node159x86). It has not
  yet failed a step by itself, but it is the same pressure class as the Aug 16/17
  gspo Ray-OOM. Watch item.

## Honesty notes

- Log analysis only. No NPU hardware; nothing was executed against verl. Claims
  about job history come from `gh api` run/job queries (verl-project/verl) and
  the work clone's git history at origin/main (cfacd76a).
- Baseline (B) values are quoted from the check step's own output; the baseline
  files themselves are on the runner and were not readable from here.
- `state/pending-prs/` does not exist yet (no draft PRs opened by this watch to
  date).
