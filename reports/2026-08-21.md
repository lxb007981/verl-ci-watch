# verl nightly Ascend CI watch — 2026-08-21

Window: `nightly_ascend.yml` runs of 2026-08-20 (17:30 / 18:28 UTC). Two runs
failed (32398034103, 32403396321), one cancelled (32409380457). All four failed
jobs were analyzed; **all four are `check_npu.py` baseline-metric regressions —
no VERL_BUG tonight, no draft PRs opened.** In every job training ran to
completion with in-band accuracy; the failures are all in the final checking
script, and in every case the *current* run is faster than the (stale) baseline.

Good news carried over from yesterday: the gspo-qwen3-8b FSDP2 `NGPUS_PER_NODE`
VERL_BUG was fixed upstream by #7456 (merged 2026-08-20 13:30 UTC) — the
pending draft is superseded and was **not** opened. See "Draft PRs opened".

## Summary

| job | run | verdict | action |
|---|---|---|---|
| nightlyCI_gspo-qwen3-30b-megatron-vllm_ascend | 32403396321 (job 96536677864) | BASELINE_REGRESSION | regenerate `baseline_gspo_qwen3_30b_megatron_npu.txt` on the runner |
| nightlyCI_dapo-moonlight-16b-megatron-vllm_ascend | 32398034103 (job 96519751529) | BASELINE_REGRESSION | regenerate baseline (day 2 in watch; failing since ≥Aug 13) |
| nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend | 32398034103 (job 96519751620) | BASELINE_REGRESSION | regenerate baseline (day 2 in watch; failing since ≥Aug 13) |
| nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend | 32398034103 (job 96519751745) | BASELINE_REGRESSION | regenerate baseline (day 2 in watch; failing since ≥Aug 13) |

## New failures

### nightlyCI_gspo-qwen3-30b-megatron-vllm_ascend — run 32403396321, job 96536677864

First completed execution of this job in this watch's window (the Aug 19 19:29
run skipped it after the gspo-8b job failed fast). Runner
`linux-aarch64-a3-16-z2d58` (label `linux-aarch64-a3-16`).

Training completed cleanly — 15/15 steps in 23:15
(`r32403396321_nightlyCI_gspo-qwen3-30b-megatron-vllm_ascend.log:4339`), final
step metrics healthy: `critic/score/mean:0.90625`, `critic/rewards/mean:0.90625`,
`response/aborted_ratio:0.0` (`...log:4351`).

The failing step is the checking script
(`check_npu.py --log run_gspo_qwen3_30b_megatron_npu.log --base .../baseline_gspo_qwen3_30b_megatron_npu.txt`):

`...log:4382-4392`
```
【性能，变化率<=5%】perf/throughput  | A=33.549691 B=29.115384 | 变化=15.23%
【性能，变化率<=5%】timing_s/step    | A=90.368034 B=102.221900 | 变化=-11.60%
AssertionError: 指标校验失败，不达标项：
  - perf/throughput 变化率 15.23%，超出阈值5%
  - timing_s/step 变化率 -11.60%，超出阈值5%
```
Memory (`差值=0.586`, threshold 1) and both accuracy metrics (rewards `差值=0.0023`,
probs_diff `变化=0.90%`) are comfortably in band (`...log:4382,4385,4386`).

**Verdict BASELINE_REGRESSION.** Only the two perf metrics are out, and both in
the *faster* direction (+15% throughput, −12% step time) — the run is strictly
better than a stale baseline. No verl commit in the last 4 days touches
`tests/special_npu/nightly_ci_ascend/run_gspo_qwen3_30b_megatron_npu.sh` or the
megatron trainer paths in a way that would plausibly produce this; and the same
"faster than baseline" signature appears in all three sibling jobs below, which
points at a shared environment/image change predating the baselines rather than
a per-job verl regression. Upstream dedup: `gh search` for "check_npu baseline",
"指标校验失败", "ascend nightly baseline" and "gspo qwen3 30b" — no matching open
issue/PR.

Action: regenerate `baseline_gspo_qwen3_30b_megatron_npu.txt` on the runner.
`check_npu.py` and all `baseline_*.txt` live only under `/root/.cache/nightly_log/`
(the workflow step `cd`s there first, `.github/workflows/nightly_ascend.yml:254`);
`find` over the repo returns no such file — nothing to PR.

Secondary (watch item, not a verdict driver): after the last step,
`RuntimeError: DataLoader worker (pid 46525) is killed by signal: Killed`
(`...log:4348`) and `Final validation metrics: None` (`...log:4352`), with
`actor/perf/cpu_memory_used_gb:1124.4` (`...log:4351`) — host RAM near capacity
on the a3-16 host too. It did not fail any step.

## Still failing

All three carried over from `state/seen-failures.json`
(`check-npu-metric-validation-failed`, firstSeen 2026-08-19, failing identically
in every completed run since ≥Aug 13). Training completed in all three tonight;
each failed only in the `check_npu.py` step.

- **nightlyCI_dapo-moonlight-16b-megatron-vllm_ascend — still failing, day 2 since
  first seen.** Out-of-band: throughput +8.63% (A=94.483865 B=86.976111),
  step +5.08% (A=433.970660 B=413.003610), probs_diff −7.09%
  (`r32398034103_...dapo....log:4561-4570`). Memory (差值=0.06) and rewards
  (差值=0.013) in band. Last training step logged at `...log:4537` (`step:15`).
- **nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend — still failing, day 2 since first
  seen.** Out-of-band: throughput +45.18% (A=69.160019 B=47.638867), step −31.36%
  (A=73.466663 B=107.026360), and tonight also `critic/rewards/mean` 差值=0.064773
  (A=0.396875 B=0.332102), a hair over the 0.05 threshold
  (`r32398034103_...ppo....log:2450-2454`). Memory 差值=−0.016. Training completed
  (15/15, `...log` "Training Progress: 100%").
- **nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend — still failing, day 2
  since first seen.** Out-of-band: memory 差值=6.11 (A=45.411313 B=39.297200),
  throughput +89.49%, step −79.06%, rewards 差值=1.25 (A=0.398250 B=−0.855600),
  probs_diff −76.46% (`r32398034103_...grpo_qwen3_vl....log:2555-2559`). The
  reward *scale* gap (A≈+0.40 vs B≈−0.86) means the baseline was recorded under a
  different reward/config regime, not that accuracy drifted.

**Day-over-day stability is the deciding evidence.** A-values barely move
between nights while the gap to B stays enormous — tonight's runs are the steady
state, the baselines are the outliers:

| job | metric | A (Aug 18) | A (Aug 20) | B (baseline) |
|---|---|---|---|---|
| qwen3_vl_8b | perf/throughput | 164.241863 | 166.081873 | 87.646000 |
| qwen3_vl_8b | timing_s/step | 87.837079 | 85.864276 | 410.057200 |
| qwen3_vl_8b | critic/rewards/mean | 0.384750 | 0.398250 | −0.855600 |
| ppo-qwen3-8b | perf/throughput | 68.556178 | 69.160019 | 47.638867 |
| ppo-qwen3-8b | timing_s/step | 73.782400 | 73.466663 | 107.026360 |
| ppo-qwen3-8b | max_mem_alloc_gb | 22.215596 | 22.215595 | 22.231838 |
| dapo-16b | perf/throughput | 97.454485 | 94.483865 | 86.976111 |
| dapo-16b | timing_s/step | 443.701911 | 433.970660 | 413.003610 |
| dapo-16b | critic/rewards/mean | −0.861185 | −0.845270 | −0.857914 |

(Aug 18 A-values quoted from `reports/2026-08-19.md`. ppo's memory is identical
to five decimals across nights — run-to-run variance is ~0.1–3%, versus a 5–90%
gap to baseline.)

Action (CI admins, all three): regenerate the `baseline_*.txt` files on the
runner. Nothing in verl to fix. Observation for maintainers, deliberately **not**
auto-drafted: the baselines are unversioned runner-side artifacts, so this class
of red cannot be fixed or reviewed through the repo; moving them into
`tests/special_npu/nightly_ci_ascend/` would make baseline refreshes reviewable.

## Draft PRs opened

None tonight. No job came back VERL_BUG.

**Carried-over update — pending draft `ci-fix/gspo-qwen3-8b-8npu` CLOSED as
superseded, no PR opened.** Yesterday's VERL_BUG
(`ValueError: Total available GPUs 8.0 < desired 16` in the gspo-qwen3-8b FSDP2
job after #7444 moved it to an 8-NPU runner) was fixed upstream by
**#7456** "[ci] chore: Update ascend ci image" (commit `6cbca9ce`, merged
2026-08-20 13:30 UTC), which changes exactly the line our branch changed:

```diff
-NGPUS_PER_NODE=${NGPUS_PER_NODE:-16}
+NGPUS_PER_NODE=${NGPUS_PER_NODE:-8}
```

Verified against `origin/main`
(`tests/special_npu/nightly_ci_ascend/run_gspo_qwen3_8b_fsdp2_npu.sh:25`).
Opening our draft now would be a duplicate, so it was not pushed as a PR; the
fork branch is left untouched and `state/pending-prs/ci-fix_gspo-qwen3-8b-8npu.md`
has been annotated CLOSED. Note the job itself was **skipped** in both of
tonight's runs, so the fix is not yet exercised by a nightly.

## Infra noise

- 32409380457 — cancelled 2026-08-20 19:40Z ('0 19' slot); no jobs to analyze.
- Run 32398034103: `nightlyCI_grpo-qwen3-30b-megatron-sglang_ascend` succeeded;
  the remaining 9 jobs (incl. gspo-qwen3-8b, the two fsdp2/qwen3_5_2b jobs and
  all quick_start jobs) were skipped, not failed.
- Run 32403396321: 5 quick_start/sglang jobs succeeded; 7 skipped.
- Shutdown-time `RuntimeError: DataLoader worker ... killed by signal: Killed`
  in 3 of 4 failed jobs (gspo-30b, dapo, ppo), with
  `actor/perf/cpu_memory_used_gb` ≈ 1124 GB on a3-16 and similar on a2b3-8 —
  host RAM running near capacity. Now seen on both pools. Still has not failed a
  step by itself; watch item (same pressure class as the Aug 16/17 gspo Ray-OOM).
- gspo-qwen3-8b job did not execute tonight (skipped in both runs) — the #7456
  fix is unverified on hardware.

## Honesty notes

- Log analysis only. No NPU hardware; nothing was executed against verl. Git
  history claims are from the work clone at `origin/main` (`6cbca9ce`); job/run
  history from `gh api` queries against verl-project/verl.
- Baseline (B) values are quoted from the check step's own output; the baseline
  files live on the runner and were not readable from here.
- Aug 18 A-values in the comparison table are quoted from this watch's own
  `reports/2026-08-19.md`, not re-derived from logs (that log set is not on this
  machine).
