# verl nightly Ascend CI watch — 2026-09-10

Window: 2026-09-09 17:11–19:12 UTC runs on `main` = `1252cc71`. Two failed
jobs, both analyzed below. No cancelled runs. One piece of good news:
**the fsdp2 mbridge clone collision is fixed upstream** (verl #7799) and
verified green on its first night — see *Still failing*.

## Summary

| job | run | verdict | action |
|---|---|---|---|
| [nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend](https://github.com/verl-project/verl/actions/runs/34393664777/job/102607932124) | [34393664777](https://github.com/verl-project/verl/actions/runs/34393664777) | KNOWN (new signature; vllm-ascend sleep-mode growth, [#9563](https://github.com/vllm-project/vllm-ascend/issues/9563)) | none in verl — watch for recurrence |
| [nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend](https://github.com/verl-project/verl/actions/runs/34381420083/job/102566943653) | [34381420083](https://github.com/verl-project/verl/actions/runs/34381420083) | INFRA (known signature, 2nd occurrence) | none in verl; diagnostic recommendation below |

## New failures

### nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend — [run 34393664777](https://github.com/verl-project/verl/actions/runs/34393664777), [job 102607932124](https://github.com/verl-project/verl/actions/runs/34393664777/job/102607932124)

**NPU OOM in the vLLM sleep-mode `wake_up` during the post-step weight
update**, after a healthy step 1–4 of 5. First non-INFRA failure of this job
(history: missing-weights INFRA Aug 19; no failure since — verified green
Sep 4, Sep 5 and Sep 8 nights, no failure recorded Sep 6/7).

Training was healthy through step 4 (throughput 35–41 tok/s, probs_diff
~0.0055, `max_memory_allocated` flat at 9.36 GB). At 19:42:13, in
`on_step_end` → weight update, one colocated vLLM server died reallocation
time (`r34393664777_nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend.log:2949`):

```
terminate called after throwing an instance of 'std::runtime_error'
  what():  aclrtMallocPhysical failed with acl error code: 207001(OOM: Out of Memory, allocation failed) /vllm-ascend/csrc/camem_allocator.cpp:67
(EngineCore) ERROR [multiproc_executor.py:284] Worker proc VllmWorker-1 died unexpectedly, shutting down executor.
```

The verl-side chain (log:2957–3091) ends inside vLLM:

```
trainer_base.py:465 fit → on_step_end → trainer_sync.py:38
→ checkpoint_engine/base.py:514 (backend="naive")
→ engine_workers.py:816  await self.rollout.resume(tags=["kv_cache"])
→ vllm_rollout.py:202    wake_up(tags=tags)
→ vllm core.py:776 wake_up → collective_rpc → worker died (CAMEM alloc OOM)
```

Configuration: `free_cache_engine=True` (vLLM `--enable_sleep_mode`),
`gpu_memory_utilization=0.4`, 4 vLLM servers × TP=2 colocated with the
FSDP-Turbo actor on 8 NPUs, `update_weights_bucket_megabytes=6144`; image
ships `vllm_ascend 0.23.1.dev20+gffb20af60`.

**Why not a verl regression.** `7cb65014..1252cc71` = 4 commits: #7764
(`1252cc71`, Mooncake completion-slot drain) is the only one near the path,
but its changed lines live in the `effective_mode != "naive"` branch
(`checkpoint_engine.send_weights`, engine_workers.py:757–765) — the traceback
frame at `base.py:514` proves the **naive** branch ran, so #7764's code never
executed. The other 3 (#7788 GLM recipe, #7799 fsdp2 workflow, #7797 AMD
tests) are inert for this job. Wider window: the sleep/wake_up orchestration
last changed in #7115 (`c2429f29`), well before the green streak.

**Upstream match.** vllm-ascend
[#9563](https://github.com/vllm-project/vllm-ascend/issues/9563) (OPEN,
*triage review*): "sleep后显存未完全回收，重复多轮sleep/wake_up后显存持续增长
最终oom" — after sleep, device memory is not fully reclaimed through
CaMemAllocator (no `empty_cache`), each `wake_up` leaves ~240 MB extra
(workspace) allocated, and repeated sleep/wake_up rounds grow device memory
until OOM. The maintainer-pointed fix PR
[#7709](https://github.com/vllm-project/vllm-ascend/pull/7709) (gc in sleep
mode) merged 2026-03-28 — *before* the issue was filed — and the reporter
confirms they have it and still see the growth. Tonight fits the profile:
4 successful sleep/wake_up cycles, then the ~5th `wake_up` OOMs —
cumulative-growth-then-tip-over on a 5-step nightly that runs marginally by
design.

**Verdict: KNOWN** (dependency-origin, tracked upstream at vllm-ascend#9563).
No verl-side action on first occurrence — the only verl "workaround" would be
dropping `free_cache_engine=True` from this job, which would remove the CI's
sleep-mode coverage entirely (maintainer call, and no verl code changed).
**If it recurs:** a human comment on #9563 citing this nightly as a clean
reproducer (colocated GRPO Qwen3.5-2B, 4×TP2 replicas, `free_cache_engine=True`,
util 0.4, OOM at the 4th–5th `wake_up`, `camem_allocator.cpp:67`) would help
prioritization. Validation: log analysis only, no NPU hardware.

### nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend — [run 34381420083](https://github.com/verl-project/verl/actions/runs/34381420083), [job 102566943653](https://github.com/verl-project/verl/actions/runs/34381420083/job/102566943653)

**Recurrence (2nd) of the empty-tee-log INFRA signature** — training healthy,
the checking script's parse found zero step lines in the tee'd log file.

Training completed a healthy 15/15 (final val reward 0.4897; probs_diff
0.0055–0.0058 dead-on; `max_memory_allocated` plateaued at 40.25 GB from
step 7; the usual benign teardown DataLoader-worker SIGKILL after the bar —
log:2846–2860). The check step then fetched its two files from
raw.githubusercontent.com — **both curls stalled ~14 min at 0 bytes**
(17:48:39→18:02, retries eventually succeeded; the cluster-egress degradation
pattern again) — and `check_npu.py` died at parse (log:3731):

```
File "/root/.cache/nightly_log/check_npu.py", line 45, in parse_log
    raise ValueError("日志中无有效step")
ValueError: 日志中无有效step
```

All 16 step lines are present in the Actions-captured tee **stdout**, so the
`python3 … | tee file` pipeline (verl's
`tests/special_npu/run_qwen3_vl_8b_Instruct_fsdp2_npu.sh:133`, unchanged)
forwarded everything; the **file** the checker read had no step lines. The
training step exited 0 — under `set -xeuo pipefail` that means `tee` itself
exited 0, i.e. it flushed and closed the file normally. Content is lost
between tee's file writes and the check's read ~14 min later, on the k8s
custom-container runner; exact mechanism not pinnable from the CI log (same
conclusion as occurrence 1).

**Not a one-off anymore**: 2026-09-03 ([run 33659656850](https://github.com/verl-project/verl/actions/runs/33659656850))
and tonight, both times this job, both times on egress-degraded evenings,
while the sibling jobs' tee'd logs parse cleanly on 12+ intervening nights.

**Verdict: INFRA.** No verl action — no verl code path implicated. Standing
recommendation (recipe/workflow-side, human's call — deliberately *not*
pushed as a branch under verl's no-busywork PR policy): add one line to the
check steps in `nightly_ascend.yml`, before `python check_npu.py` —
`ls -l` + `wc -c` of the tee'd log — to pin *where* the content is lost
(empty vs truncated vs replaced file) on the next recurrence. Same hardening
family as the standing `--retry-all-errors` curl note.

**Masked by this failure:** the stale-baseline memory check would have failed
again (plateau 40.25 GB vs B=45.43, ~−5.2 GB, threshold 1 — the documented
post-#7428 band). The recipe baseline regen is now **9 days outstanding**;
this job stays red on every night where the log file survives. Also not
observable tonight: the EADDRINUSE race (#6677) and the HBM-ECC signature —
engines started cleanly, no device errors.

## Still failing

- `nightlyCI_grpo_qwen3_5_2b_fsdp2_vllm_ascend` **mbridge clone collision — RESOLVED UPSTREAM** via verl [#7799](https://github.com/verl-project/verl/pull/7799) (`bf84b14b`, merged 2026-09-09 ~03:35 UTC): removes the "Clone Megatron Bridge" step + PYTHONPATH export. Verified green first night — job `success` in [run 34387178376](https://github.com/verl-project/verl/actions/runs/34387178376) (main `1252cc71`) alongside gspo-30b and all four quick_start jobs. Our branch `ci-fix/ascend-a3-mbridge-clone-collision` is **superseded — no PR should be opened from it**.
- `nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend` metric check (stale post-#7428 baseline): not observable tonight (masked by the empty-log INFRA above); memory plateau 40.25 GB confirms the forecast band; recipe baseline regen 9 days outstanding — the job stays red until then.
- `nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend` no_sync default flip (#7458): job skipped tonight; main `1252cc71` still ships `use_no_sync_for_gradient_accumulation: false` in all 4 YAML spots — fix not landed, branch absent from fork, audit retained for reuse.
- `nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend` rewards/throughput flaps: job green tonight ([run 34381420083](https://github.com/verl-project/verl/actions/runs/34381420083)); signatures dormant.
- `nightlyCI_gspo-qwen3-30b-megatron-vllm_ascend` memory flap: job green tonight ([run 34387178376](https://github.com/verl-project/verl/actions/runs/34387178376)); threshold-bound flap class.
- `nightlyCI_grpo-qwen3-30b-megatron-sglang_ascend` rewards flap (2026-09-01 one-off): job skipped tonight; dormant.
- EADDRINUSE vLLM dist-init race (verl [#6677](https://github.com/verl-project/verl/issues/6677), tracked at vllm#39015): did not recur — engines started cleanly tonight; #6677 still OPEN.
- HBM multi-bit ECC on 192.168.9.113 device 1: did not recur tonight; runner-admin flag stands (2 events in 12 days as of Sep 4).
- dapo-moonlight-16b job: still disabled in the workflow (absent from all three runs again).
- Router-replay #7106 regression: stays resolved via the #7786 revert — all Megatron jobs green/skipped tonight, third consecutive clean night.

## Fix branches pushed

**None pushed today** — neither failure is a verl bug.

Carried-over branch status:

- [`ci-fix/ascend-a3-mbridge-clone-collision`](https://github.com/lxb007981/verl/tree/ci-fix/ascend-a3-mbridge-clone-collision) (head `bf8c4495`) — **SUPERSEDED** by upstream [#7799](https://github.com/verl-project/verl/pull/7799); verified green in [run 34387178376](https://github.com/verl-project/verl/actions/runs/34387178376). Do not open a PR; recommend the owner discards the branch. Audit updated.
- [`ci-fix/router-replay-empty-registry`](https://github.com/lxb007981/verl/tree/ci-fix/router-replay-empty-registry) (head `a56f3ec7`) — superseded by the upstream #7106 revert (#7786); no PR, recommend discard (unchanged from previous days).
- `ci-fix/fsdp-no-sync-default-true` — still absent from the fork (deleted Sep 4, treated as an out-of-band human decision); audit + PR body retained for reuse; main still ships the four `false` YAMLs.

## Infra noise

- No cancelled runs in the window.
- [Run 34387178376](https://github.com/verl-project/verl/actions/runs/34387178376) (18:08) fully green — first night on #7799's workflow; veomni-30b, gspo-8b, grpo-30b-sglang, qwen3_vl and fsdp_turbo jobs skipped in it (staggered nightly schedules).
- Cluster egress to raw.githubusercontent.com degraded ~17:48–18:02 UTC: both check-step curls in the qwen3_vl job stalled ~14 min at 0 bytes before retries succeeded. Same family as the Sep 3/Sep 4 blips; the standing `--retry-all-errors` hardening candidate remains unprepared.
