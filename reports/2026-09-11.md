# verl nightly Ascend CI watch — 2026-09-11

Window: 2026-09-10 17:12–20:42 UTC runs on `main` = `68c9ac35`. One failed
job — a **known INFRA signature, 3rd occurrence** (dedup hit, not
re-analyzed from scratch; tonight's evidence below confirms the match and
adds one new datum). No cancelled runs; both sibling nightly runs
([34512456074](https://github.com/verl-project/verl/actions/runs/34512456074),
[34518923074](https://github.com/verl-project/verl/actions/runs/34518923074))
fully green.

## Summary

| job | run | verdict | action |
|---|---|---|---|
| [nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend](https://github.com/verl-project/verl/actions/runs/34506778944/job/102970797276) | [34506778944](https://github.com/verl-project/verl/actions/runs/34506778944) | INFRA (known signature `check-npu-log-empty-no-valid-step`, 3rd occurrence, day 8 since first seen) | none in verl; recipe-side diagnostic (ls/wc of tee'd log) now overdue |

## New failures

None — the single failed job matched an existing signature in
`state/seen-failures.json` (see *Still failing*).

## Still failing

- **`check-npu-log-empty-no-valid-step`** — still failing, day 8 since first
  seen (2026-09-03), 3rd occurrence tonight
  ([run 34506778944](https://github.com/verl-project/verl/actions/runs/34506778944),
  [job 102970797276](https://github.com/verl-project/verl/actions/runs/34506778944/job/102970797276),
  main `68c9ac35`). Training completed a healthy 15/15 (final val reward
  0.4717; probs_diff 0.0055–0.0057 dead-on; `response/aborted` 0.0; the
  usual benign teardown DataLoader-worker SIGKILL after the bar —
  `r34506778944_nightlyCI_grpo_qwen3_vl_8b_Instruct_fsdp2_vllm_ascend.log:2767`).
  The training step exited 0; the job died only in the checking step, where
  `check_npu.py` again raised `ValueError: 日志中无有效step` at `parse_log`
  (log:2807–2808) while all 16 step lines (15 train + the step:0 val line)
  are present in the Actions-captured tee **stdout** — the tee'd **file** the
  checker read had none. The tee itself is verl's unchanged
  `tests/special_npu/run_qwen3_vl_8b_Instruct_fsdp2_npu.sh:133`
  (`"$@" | tee $LOG_DIR/$SCRIPT_NAME.log`).
  **New datum tonight — egress correlation broken:** unlike occurrences 1–2,
  the check-step curls were *fast* (~1 s each, full speed, 17:49:09→17:49:10)
  and only ~6 s elapsed between the training step's exit (17:49:04) and the
  check's read. Slow egress is therefore **not** a prerequisite; the file
  content is lost (or never durably written) even on an immediate read on
  the k8s custom-container runner. verl exonerated again: window
  `1252cc71..68c9ac35` = 3 commits (#7808 vllm test coverage, #7827 Megatron
  Ascend image config, #7762 trainer-ckpt async warmup skip), none on this
  job's path — and the failing step is not verl code at all. Verdict
  **INFRA**, no verl action. With 3 occurrences in 9 days, the standing
  recipe-side recommendation (add `ls -l` + `wc -c` of the tee'd log inside
  the check step of `nightly_ascend.yml`, before `python check_npu.py`, to
  pin empty-vs-truncated-vs-replaced on the next recurrence) is now
  overdue — human's call, deliberately not pushed as a verl branch.
- **`check-npu-metric-validation-failed`** (stale post-#7428 baseline) — not
  observable tonight (masked by the empty log above). Memory plateau
  41.470267 GB from step 5, squarely inside the documented post-#7428 band
  ~39–42.4 and −3.96 GB below the stale B=45.43 (threshold 1) — the memory
  check would have failed again had the log parsed. Recipe baseline regen
  now **10 days outstanding**; the job stays red on every night where the
  log file survives.
- EADDRINUSE vLLM dist-init race (verl [#6677](https://github.com/verl-project/verl/issues/6677),
  tracked at vllm#39015): did not recur — engines started cleanly tonight;
  #6677 still OPEN.
- HBM multi-bit ECC on 192.168.9.113 device 1: did not recur; no device
  errors tonight (the `ERR99999` at log:2773 is the app-exception wrapper
  of the teardown SIGKILL, not an ECC event).
- `nightlyCI_grpo_qwen3_5_2b_fsdp_turbo_vllm_ascend` vLLM sleep-mode CaMem
  growth (vllm-ascend [#9563](https://github.com/vllm-project/vllm-ascend/issues/9563)):
  job green tonight (both sibling runs fully green); watching for
  recurrence.
- `nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend` no_sync default flip (#7458):
  main still ships `use_no_sync_for_gradient_accumulation: false` in all 4
  YAML spots (`verl/trainer/config/engine/fsdp.yaml:61` +
  `_generated_ppo_trainer.yaml:46,254,573`); fix not landed, branch absent
  from fork, audit retained for reuse.
- `nightlyCI_gspo-qwen3-30b-megatron-vllm_ascend` / grpo-30b-sglang
  rewards/memory flaps, `nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend` flaps:
  dormant; green in tonight's runs.
- dapo-moonlight-16b job: still disabled in the workflow (commented out in
  `nightly_ascend.yml:150–198`).
- Router-replay #7106 regression: stays resolved via the #7786 revert; no
  Megatron failures tonight.

## Fix branches pushed

**None pushed today** — the only failure is INFRA (runner-side), not a verl
bug.

Carried-over branch status (unchanged from previous days):

- [`ci-fix/ascend-a3-mbridge-clone-collision`](https://github.com/lxb007981/verl/tree/ci-fix/ascend-a3-mbridge-clone-collision) — **SUPERSEDED** by upstream [#7799](https://github.com/verl-project/verl/pull/7799); do not open a PR; recommend the owner discards the branch.
- [`ci-fix/router-replay-empty-registry`](https://github.com/lxb007981/verl/tree/ci-fix/router-replay-empty-registry) — superseded by the upstream #7106 revert (#7786); no PR, recommend discard.
- `ci-fix/fsdp-no-sync-default-true` — still absent from the fork (out-of-band human decision); audit + PR body retained; main still ships the four `false` YAMLs.

## Infra noise

- No cancelled runs in the window.
- [Run 34512456074](https://github.com/verl-project/verl/actions/runs/34512456074) (18:08) and [run 34518923074](https://github.com/verl-project/verl/actions/runs/34518923074) (19:11) fully green — so the empty-log signature again hit only the 17:12 run's qwen3_vl job, while every sibling job's tee'd log parsed cleanly (15+ parse-clean nights for siblings across the three occurrences).
- No cluster-egress degradation tonight (check-step curls at full speed) — the Sep 3/9 egress-stall pattern did not reappear, yet the log-content loss did; the runner-side `ls -l + wc -c` diagnostic is the fastest way to pin the mechanism.
