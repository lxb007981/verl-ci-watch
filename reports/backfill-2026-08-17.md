# verl nightly Ascend CI watch — backfill-2026-08-17

## Summary

| job | run | verdict | action |
|---|---|---|---|
| nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend | 31966689363 (job 95212828118) | INFRA | none — upstream already moved the job off the a3-16 runner (b5a35481 / #7444, merged Aug 17); watch item recorded for tonight |

## New failures

### nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend — run 31966689363, job 95212828118

Failed 2026-08-16 19:46:01Z (37 min into the job), on runner
`linux-aarch64-a3-16-z2d58-runner-vnn94` (CANN 9.1.0, aarch64), running
`tests/special_npu/nightly_ci_ascend/run_gspo_qwen3_8b_fsdp2_npu.sh`
(NGPUS_PER_NODE=16, Qwen3-8B, fsdp2, vLLM rollout TP=2).

**Real error — host-RAM OOM, not an NPU/device fault, and not a Python code
fault.** During `actor_rollout_wg.init_model()` (weight loading, before training
step 1), Ray's memory monitor killed 2 of the 16 `WorkerDict` workers:

`r31966689363_nightlyCI_gspo-qwen3-8b-fsdp2-vllm_ascend.log:2073`
```
ray.exceptions.OutOfMemoryError: 2 worker(s) were killed due to the node running low on memory.
Memory on the node (IP: 10.0.0.43, ...) was 498.84GB / 512.00GB (0.974302)
```
`...log:2074`
```
OOM kill reason: Memory usage 522888658944B exceeded threshold of 522268016640B (95.0% of 549755813888B total)
```
`...log:2132-2135` — top memory users were all 16 `ray::WorkerDict.actor_rollout_init_model`
processes at ~29.1–29.5 GB RSS each. The tail of the weight load stalled at the
same moment (334/399 shards at 23 it/s, then 345–346/399 at ~2–2.6 s/it,
`...log:2003-2014`) — consistent with swap thrash as all 16 ranks hit peak RSS
together. Then `[ERROR] ERR99999 UNKNOWN applicaiton exception` (`...log:2144`)
and exit code 1 (`...log:2151`).

**Root cause arithmetic:** each WorkerDict rank holds a full Qwen3-8B bf16 host
copy (~16.4 GB, HF `from_pretrained` before FSDP2 sharding) plus a per-process
torch_npu/CANN context → ~29.3 GB × 16 ranks ≈ 467 GB, plus 13.4 GB of idle Ray
workers and Ray/OS overhead, on a 512 GiB a3-16 host with Ray's kill threshold
at 95% (~522 GB). The job's process layout simply sits at ~91–97% of the host's
RAM at init; any drift in per-process overhead tips it over.

**Verdict rationale — INFRA (runner host-memory sizing), not VERL_BUG:**
- No verl code on the fsdp2/vLLM init path changed in the 4 days before the run:
  `git log --since=2026-08-08 -- verl/workers verl/single_controller` shows only
  megatron/sglang/reward/LoRA changes; the gspo script itself was last touched
  08-04 (b8350f6e, log-dedup only) and the job block in `nightly_ascend.yml` was
  unchanged since its creation (c1be8972, 07-07).
- The runner environment did churn: the moving-tag a3 CI image was refreshed in
  #7293 (09ac3725, 08-13, adds `triton-ascend==3.2.2`), and the failure hit the
  first '0 19' run after that — a nudge in per-process overhead on a layout
  already at ~93% of node RAM is sufficient to explain it without any verl bug.
- Corroborating upstream behavior: the very next day, PR #7444 (b5a35481,
  08-17, "Move lightweight tests to A2 instances") moved this exact job from
  `linux-aarch64-a3-16` / `latest-vllm-a3-ubuntu` to
  `linux-aarch64-a2b3-8` / `latest-vllm-910b-ubuntu` — treated as capacity
  reallocation, not a code fix.

Dedup: day 1 of the watch — `state/seen-failures.json` did not exist; this is
the first entry. Upstream issue/PR search via `gh search` and web search was
permission-blocked in this session (see honesty notes); the #7444 evidence above
comes from the local clone's git history.

## Still failing

None — first day of the watch.

## Draft PRs opened

None. Verdict for the only failed job is INFRA; nothing to fix in verl for the
Aug 16 failure, and the job no longer runs on the runner class that OOMed.

## Infra noise

Cancelled runs in the window (no analysis, per procedure):
- 31960599846 — cancelled 2026-08-16 17:06Z (schedule, main)
- 31963453856 — cancelled 2026-08-16 18:03Z (schedule, main)
- 32050452602 — cancelled 2026-08-17 17:28Z (schedule, main)
- 32055042929 — cancelled 2026-08-17 18:28Z (schedule, main)
- 32060816134 — cancelled 2026-08-17 19:31Z (schedule, main) — the '0 19' slot,
  i.e. this job's new schedule; consecutive cancellations on Aug 17 suggest
  runs were being cancelled/superseded upstream (unverifiable here: `gh` blocked)

## Watch items (not failures yet — no action taken)

1. **NGPUS default now stale after #7444.** `run_gspo_qwen3_8b_fsdp2_npu.sh:25`
   defaults `NGPUS_PER_NODE=16`, but the job now runs on
   `linux-aarch64-a2b3-8` (`nightly_ascend.yml:612`) and the workflow does not
   export `NGPUS_PER_NODE`. The sibling job on the same runner class
   (`nightlyCI_ppo-qwen3-8b-fsdp-vllm_ascend`) uses `trainer.n_gpus_per_node=8`
   (`run_ppo_qwen3-8b_fsdp_npu.sh:57`). Tonight's run will likely fail at Ray
   resource placement (16 NPU bundles requested on an 8-NPU host). If it does,
   the fix is a one-liner (`NGPUS_PER_NODE:-8` or a workflow env) — deliberately
   not drafted today: predicting a failure that has not produced a run is below
   the bar for an auto-draft, and the intent of #7444's author is unknown.

## Honesty notes

- Log analysis only; no NPU hardware was available and nothing was executed.
- Tool blocks in this unattended session (noted, not fought, per hard rule 4):
  `git fetch`/`git push` to the work clone, all `gh` CLI calls, and WebSearch
  were denied by the permission layer despite the allowlist in
  `.claude/settings.json` — so upstream issue/PR search and fork push were
  impossible. Had any job come back VERL_BUG, the branch would have been
  prepared locally with the PR body recorded, and the push/PR step reported as
  blocked. The work clone was used at its existing state (main = a9f10cd5,
  which contains commits through 08-17, i.e. at-or-after the run's SHA; run
  history was cross-checked via the fetched `runs.json`/logs).
